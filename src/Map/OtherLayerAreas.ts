const OTHER_AREAS: {
  prefix: string;
  offset: [number, number];
  overlay: [[number, number], [number, number]];
}[] = [
  {
    prefix: "sunken_stadium",
    offset: [0, 8000],
    overlay: [
      [-36000, -34650],
      [-50500, -15986],
    ],
  },
];

export default OTHER_AREAS;
